import React from 'react';

function InnerPage() {
  return <div style={styles.container}>This is Inner Page</div>;
}

const styles = {
  container: {
    padding: '1rem',
  },
};

export default InnerPage;